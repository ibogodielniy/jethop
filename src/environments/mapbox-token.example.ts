// Template for the local Mapbox token file.
//
// Setup:
//   1. Copy this file to `mapbox-token.ts` in the same folder.
//   2. Paste your Mapbox PUBLIC token (starts with `pk.`) below.
//   3. `mapbox-token.ts` is gitignored, so your token is never committed.
//
// Get a token at https://account.mapbox.com/access-tokens/ and restrict it
// by URL for production use.
export const MAPBOX_TOKEN = 'pk.your-mapbox-public-token-here';
